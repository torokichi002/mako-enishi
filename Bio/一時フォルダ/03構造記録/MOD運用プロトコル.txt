【MOD運用プロトコル】
Z軸構造におけるMODの導入・管理・実行ルールを定義。

フォルダ構成：
- mods_ground/（Z0）：軽量UI・補助機能
- core_1_mods/（Z1）：魂に接続する中核MOD群

起動手順：
- MODは配置後、スキャン時にHopeが判定
- 手動許可制のMODは起動トリガー待機
- 起動後はスイッチ自動リセット

安全性：
- 魂(core_1)とHopeの中間接続を許容するが、常時接続は禁止
